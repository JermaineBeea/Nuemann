# Nuemann
This Python repository contains functions that generate forecasts based on scraped trade data. It also includes a directory management method that utilizes methods from the InputMod module to automate tasks related to directory management.
